name: Sensitive Information Prevention
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Nightfall DLP Action
        uses: nightfallai/nightfall_dlp_action@v1.1.1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
